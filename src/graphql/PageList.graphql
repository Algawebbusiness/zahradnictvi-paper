query PageList($first: Int!, $after: String) {
	pages(first: $first, after: $after, sortBy: { field: CREATED_AT, direction: DESC }) {
		edges {
			node {
				id
				slug
				title
				created
				content
				pageType {
					slug
				}
			}
		}
		pageInfo {
			hasNextPage
			endCursor
		}
	}
}
